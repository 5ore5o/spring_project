<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Shopping_mall">

<!-- id중복되면 안됨 -->
<select id="search" parameterType="Map" resultType="userdao"> <!-- resultType은 config.xml에 있는 alias -->
select * from user
<choose> <!-- 여러가지 형태의 쿼리문을 사용시 적용하는 태그 where+if 개선된 형태가 => choose 임 -->
	<!-- when은 안정적으로 쓰는거면 40개까지 쓴다. -->
	<when test="part==1"> <!-- 아이디 찾기 -->
	where uname=#{uname} and uemail=#{uemail} 
	</when>
	<when test="part==2"> <!-- 패스워드 찾기 -->
	where uid=#{uid} and uname=#{uname}
	</when>
	<otherwise>
		order by uidx desc
	</otherwise>
</choose>
</select>
</mapper>